<% if (post.tags && post.tags.length){ %>
  <span class="article-tag">
    <i class="fas fa-tags"></i>
	<%- list_tags(post.tags, {
	   show_count: false,
	   style: 'link',
	   class: 'article-tag'
	}) %>
  </span>
<% } %>
